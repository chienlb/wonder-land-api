<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content=device-width, initial-scale=1.0">
    <title>Thông báo khuyến mãi</title>

    <style>
        body {
            margin: 0; padding: 0;
            background: #FFF8F2;
            font-family: 'Segoe UI', sans-serif;
            color: #3A3A3A;
            line-height: 1.6;
        }

        .container {
            max-width: 580px;
            margin: 0 auto;
            background: #ffffff;
            border-radius: 14px;
            overflow: hidden;
            box-shadow: 0 4px 18px rgba(255,150,80,0.18);
        }

        /* HEADER */
        .header {
            background: linear-gradient(135deg, #FF8A3D 0%, #FFB784 100%);
            padding: 60px 40px;
            text-align: center;
        }
        .logo {
            color: #fff;
            font-size: 20px;
            letter-spacing: 3px;
            text-transform: uppercase;
            font-weight: 300;
        }

        /* CONTENT */
        .content {
            padding: 60px 40px;
        }
        .greeting {
            font-size: 26px;
            color: #FF7A29;
            font-weight: 300;
            margin-bottom: 20px;
        }
        .description {
            font-size: 15px;
            color: #4A4A4A;
            margin-bottom: 25px;
        }
        .highlight {
            color: #FF5F00;
            font-weight: 600;
        }

        /* PROMO BOX */
        .promo-box {
            background: linear-gradient(135deg, #FFF1E6 0%, #FFF7F2 100%);
            border: 2px solid #FFC7A4;
            padding: 40px;
            border-radius: 14px;
            margin-bottom: 40px;
            text-align: center;
        }
        .promo-title {
            font-size: 24px;
            font-weight: 700;
            color: #FF7A29;
            margin-bottom: 15px;
        }
        .promo-value {
            font-size: 38px;
            font-weight: 800;
            color: #FF5F00;
            margin-bottom: 10px;
            letter-spacing: 1px;
        }
        .promo-detail {
            font-size: 14px;
            color: #4A4A4A;
            margin-bottom: 10px;
        }

        /* CODE BOX */
        .code-box {
            display: inline-block;
            background: #FFF9E6;
            padding: 14px 20px;
            border-radius: 10px;
            font-size: 20px;
            font-weight: 700;
            color: #FF7A29;
            border: 2px dashed #FFB784;
            margin: 15px 0;
        }

        /* CTA */
        .action-box {
            background: #FFF4E8;
            border: 2px solid #FF8A3D;
            padding: 35px;
            border-radius: 12px;
            text-align: center;
            margin-bottom: 40px;
        }
        .action-link {
            color: #FF5F00;
            text-decoration: none;
            font-weight: 700;
            font-size: 16px;
            border-bottom: 3px solid #FF8A3D;
            padding-bottom: 3px;
            transition: .3s ease;
        }
        .action-link:hover {
            color: #FF8A3D;
            border-bottom-color: #FF5F00;
        }

        /* FOOTER */
        .footer {
            background: #FFEFE2;
            text-align: center;
            padding: 45px;
            border-top: 2px solid #FF8A3D;
        }
        .footer-text {
            font-size: 12px;
            color: #FF7A29;
        }

        @media (max-width:600px) {
            .content, .header, .footer {
                padding: 40px 25px;
            }
            .promo-box, .action-box {
                padding: 25px;
            }
        }
    </style>
</head>

<body>
<table width="100%">
    <tr>
        <td align="center" style="padding: 30px 10px;">
            <table class="container">

                <!-- HEADER -->
                <tr>
                    <td class="header">
                        <div class="logo">{{brandName}}</div>
                    </td>
                </tr>

                <!-- CONTENT -->
                <tr>
                    <td class="content">

                        <p class="greeting">🎉 Ưu đãi đặc biệt dành cho bạn! 🎉</p>

                        <p class="description">
                            Chúng tôi rất vui thông báo rằng <span class="highlight">{{promoName}}</span>  
                            đang chính thức bắt đầu! Đừng bỏ lỡ cơ hội nhận ưu đãi cực lớn ngay hôm nay.
                        </p>

                        <!-- PROMO BOX -->
                        <div class="promo-box">
                            <div class="promo-title">{{promoTitle}}</div>
                            <div class="promo-value">{{promoValue}}</div>
                            <div class="promo-detail">
                                Áp dụng cho: <b>{{applyFor}}</b>
                            </div>
                            <div class="promo-detail">
                                Hạn sử dụng: <b>{{expiredDate}}</b>
                            </div>

                            <div class="code-box">{{promoCode}}</div>
                        </div>

                        <!-- CTA -->
                        <div class="action-box">
                            <p>Sử dụng mã ngay tại đây:</p>
                            <a href="{{promoLink}}" class="action-link">Nhận ưu đãi</a>
                        </div>

                        <p class="description">
                            Hãy tranh thủ vì ưu đãi chỉ diễn ra trong thời gian ngắn!  
                            Chúc bạn có trải nghiệm tuyệt vời cùng chúng tôi 💛🔥
                        </p>

                    </td>
                </tr>

                <!-- FOOTER -->
                <tr>
                    <td class="footer">
                        <p class="footer-text">© {{year}} {{brandName}} – Tất cả quyền được bảo lưu.</p>
                    </td>
                </tr>

            </table>
        </td>
    </tr>
</table>
</body>
</html>
