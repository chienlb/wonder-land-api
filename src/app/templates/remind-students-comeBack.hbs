<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nhắc học viên quay lại học</title>

    <style>
        body {
            margin: 0; padding: 0;
            background: #F7F7FF;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Helvetica Neue', sans-serif;
            line-height: 1.6;
            color: #3a3a3a;
        }

        .container {
            max-width: 580px;
            margin: 0 auto;
            background: #ffffff;
            border-radius: 12px;
            box-shadow: 0 4px 16px rgba(108, 99, 255, 0.12);
            overflow: hidden;
        }

        .header {
            background: linear-gradient(135deg, #7C70FF 0%, #56C5FF 100%);
            padding: 60px 40px;
            text-align: center;
        }
        .logo {
            color: #fff;
            font-size: 20px;
            letter-spacing: 2px;
            text-transform: uppercase;
            font-weight: 300;
        }

        .content {
            padding: 60px 40px;
        }

        .greeting {
            font-size: 24px;
            font-weight: 300;
            color: #6A5ACD;
            margin-bottom: 30px;
        }

        .description {
            font-size: 15px;
            color: #555;
            margin-bottom: 30px;
        }

        .highlight {
            font-weight: 600;
            color: #6C63FF;
        }

        /* inactive notice */
        .inactive-box {
            background: linear-gradient(135deg, #F2F1FF 0%, #F9F8FF 100%);
            padding: 35px;
            border-radius: 12px;
            border: 2px solid #D6D1FF;
            margin-bottom: 40px;
            box-shadow: 0 3px 10px rgba(108, 99, 255, 0.10);
        }
        .inactive-title {
            font-size: 20px;
            font-weight: 500;
            color: #6A5ACD;
            margin-bottom: 15px;
        }
        .inactive-text {
            font-size: 14px;
            color: #555;
            line-height: 1.9;
        }

        /* CTA */
        .action-box {
            background: linear-gradient(135deg, #E9F3FF 0%, #F4F9FF 100%);
            padding: 35px;
            border-radius: 10px;
            border: 2px solid #66A8FF;
            text-align: center;
            margin-bottom: 40px;
        }
        .action-text {
            font-size: 14px;
            color: #555;
            margin-bottom: 15px;
        }
        .action-link {
            color: #357BFF;
            font-weight: 600;
            text-decoration: none;
            border-bottom: 2px solid #6C63FF;
            padding-bottom: 2px;
            transition: .3s ease;
        }
        .action-link:hover {
            color: #6C63FF;
            border-bottom-color: #357BFF;
        }

        .divider {
            height: 1px;
            background: linear-gradient(90deg, transparent, #6C63FF, transparent);
            margin: 40px 0;
        }

        /* Footer */
        .footer {
            background: linear-gradient(135deg, #EEF1FF 0%, #F4F7FF 100%);
            padding: 45px 40px;
            text-align: center;
            border-top: 2px solid #6C63FF;
        }
        .footer-text {
            font-size: 12px;
            color: #6A5ACD;
            margin-bottom: 10px;
        }
        .footer-divider {
            height: 1px;
            background: linear-gradient(90deg, transparent, #FFD54F, transparent);
            margin: 25px 0;
        }

        @media (max-width: 600px) {
            .content, .header, .footer {
                padding: 40px 25px;
            }
            .inactive-box, .action-box {
                padding: 25px;
            }
        }
    </style>
</head>

<body>
<table role="presentation" width="100%">
    <tr>
        <td align="center" style="padding: 30px 10px;">
            <table class="container" role="presentation">

                <!-- HEADER -->
                <tr>
                    <td class="header">
                        <div class="logo">{{brandName}}</div>
                    </td>
                </tr>

                <!-- CONTENT -->
                <tr>
                    <td class="content">

                        <p class="greeting">Xin chào <b>{{studentName}}</b>,</p>

                        <p class="description">
                            Chúng tôi nhận thấy rằng bạn đã <span class="highlight">{{inactiveDays}} ngày</span>
                            chưa quay lại học.  
                            Chúng tôi hy vọng mọi thứ với bạn vẫn ổn!
                        </p>

                        <div class="inactive-box">
                            <div class="inactive-title">Bạn đã bỏ lỡ những gì?</div>
                            <p class="inactive-text">
                                • Bài học tiếp theo của khóa <b>{{courseName}}</b>  
                                • Tiến độ hiện tại: <span class="highlight">{{completionRate}}%</span><br>
                                • Gợi ý: bạn nên hoàn thành Module <b>{{nextModule}}</b> để không bị gián đoạn.
                            </p>
                        </div>

                        <div class="action-box">
                            <p class="action-text">Bạn có thể tiếp tục học chỉ với 1 cú nhấp chuột:</p>
                            <a href="{{continueLink}}" class="action-link">Quay lại học ngay</a>
                        </div>

                        <p class="description">
                            Hãy dành ít nhất 10 phút hôm nay để tiếp tục tiến bộ.  
                            Chúng tôi luôn đồng hành cùng bạn! 💙
                        </p>

                        <div class="divider"></div>

                    </td>
                </tr>

                <!-- FOOTER -->
                <tr>
                    <td class="footer">
                        <p class="footer-text">© {{year}} {{brandName}}. Mọi quyền được bảo lưu.</p>
                        <div class="footer-divider"></div>

                        <p class="footer-text">
                            <a href="{{supportLink}}" style="color:#6A5ACD; text-decoration:none;">
                                Liên hệ hỗ trợ
                            </a>
                        </p>
                    </td>
                </tr>

            </table>
        </td>
    </tr>
</table>
</body>
</html>
