<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thông báo bài tập mới</title>

    <style>
        body {
            margin: 0; padding: 0;
            background: #F7F7FF;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Helvetica Neue', sans-serif;
            line-height: 1.6;
            color: #3a3a3a;
        }

        .container {
            max-width: 580px;
            margin: 0 auto;
            background: #ffffff;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 18px rgba(124, 112, 255, 0.12);
        }

        /* Header */
        .header {
            background: linear-gradient(135deg, #6C63FF 0%, #66C6FF 100%);
            padding: 60px 40px;
            text-align: center;
        }
        .logo {
            color: #fff;
            font-size: 20px;
            letter-spacing: 3px;
            font-weight: 300;
            text-transform: uppercase;
        }

        .content {
            padding: 60px 40px;
        }

        .greeting {
            font-size: 24px;
            font-weight: 300;
            color: #6A5ACD;
            margin-bottom: 30px;
        }

        .description {
            font-size: 15px;
            color: #555;
            margin-bottom: 30px;
        }

        .highlight {
            color: #6C63FF;
            font-weight: 600;
        }

        /* Assignment Box */
        .assignment-box {
            background: linear-gradient(135deg, #F1EFFF 0%, #F8F7FF 100%);
            padding: 35px;
            border-radius: 12px;
            border: 2px solid #D4CCFF;
            margin-bottom: 40px;
            box-shadow: 0 4px 12px rgba(108, 99, 255, 0.10);
        }
        .assignment-title {
            font-size: 20px;
            color: #6C63FF;
            font-weight: 500;
            margin-bottom: 15px;
        }
        .assignment-detail {
            font-size: 14px;
            color: #444;
            margin-bottom: 10px;
            line-height: 1.9;
        }

        /* CTA */
        .action-box {
            background: linear-gradient(135deg, #EAF3FF 0%, #F3F8FF 100%);
            padding: 35px;
            border-radius: 10px;
            border: 2px solid #66A8FF;
            text-align: center;
            margin-bottom: 40px;
        }
        .action-text {
            font-size: 14px;
            color: #555;
            margin-bottom: 15px;
        }
        .action-link {
            color: #357BFF;
            text-decoration: none;
            font-weight: 600;
            border-bottom: 2px solid #6C63FF;
            padding-bottom: 2px;
            transition: .3s ease;
        }
        .action-link:hover {
            color: #6C63FF;
            border-bottom-color: #357BFF;
        }

        .divider {
            height: 1px;
            background: linear-gradient(90deg, transparent, #6C63FF, transparent);
            margin: 40px 0;
        }

        /* Footer */
        .footer {
            background: linear-gradient(135deg, #EEF1FF 0%, #F4F7FF 100%);
            padding: 50px 40px;
            text-align: center;
            border-top: 2px solid #6C63FF;
        }
        .footer-text {
            font-size: 12px;
            color: #6A5ACD;
            margin-bottom: 12px;
        }
        .footer-divider {
            height: 1px;
            background: linear-gradient(90deg, transparent, #FFD54F, transparent);
            margin: 25px 0;
        }

        @media (max-width: 600px) {
            .content, .header, .footer {
                padding: 40px 25px;
            }
            .assignment-box, .action-box {
                padding: 25px;
            }
        }
    </style>
</head>

<body>
<table role="presentation" width="100%">
    <tr>
        <td align="center" style="padding: 30px 10px;">
            <table class="container" role="presentation">

                <!-- HEADER -->
                <tr>
                    <td class="header">
                        <div class="logo">{{brandName}}</div>
                    </td>
                </tr>

                <!-- CONTENT -->
                <tr>
                    <td class="content">

                        <p class="greeting">Xin chào <b>{{studentName}}</b>,</p>

                        <p class="description">
                            Một <span class="highlight">bài tập mới</span> đã được giao trong khóa học <b>{{courseName}}</b>.  
                            Hãy hoàn thành đúng hạn để duy trì tiến độ học tập của bạn nhé!
                        </p>

                        <!-- Assignment Info -->
                        <div class="assignment-box">
                            <div class="assignment-title">Thông tin bài tập</div>

                            <p class="assignment-detail">📘 Tên bài tập: <b>{{assignmentTitle}}</b></p>
                            <p class="assignment-detail">📝 Mô tả: {{assignmentDescription}}</p>
                            <p class="assignment-detail">⏳ Hạn nộp: <span class="highlight">{{deadline}}</span></p>
                            <p class="assignment-detail">📁 Đính kèm (nếu có): {{attachmentName}}</p>
                        </div>

                        <!-- CTA -->
                        <div class="action-box">
                            <p class="action-text">Bạn có thể xem và làm bài tại đây:</p>
                            <a href="{{assignmentLink}}" class="action-link">Làm bài ngay</a>
                        </div>

                        <p class="description">
                            Chúc bạn hoàn thành bài tập thật tốt ✨  
                            Nếu cần hỗ trợ, đừng ngần ngại liên hệ với chúng tôi!
                        </p>

                        <div class="divider"></div>

                    </td>
                </tr>

                <!-- FOOTER -->
                <tr>
                    <td class="footer">
                        <p class="footer-text">© {{year}} {{brandName}}. Mọi quyền được bảo lưu.</p>
                        <div class="footer-divider"></div>
                        <p class="footer-text">
                            <a href="{{supportLink}}" style="color:#6A5ACD; text-decoration:none;">
                                Liên hệ hỗ trợ
                            </a>
                        </p>
                    </td>
                </tr>

            </table>
        </td>
    </tr>
</table>
</body>
</html>
