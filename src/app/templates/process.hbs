<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Báo cáo tiến độ học tập</title>

    <style>
        body {
            margin: 0; padding: 0;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Helvetica Neue', sans-serif;
            background: #F7F7FF;
            color: #3a3a3a;
            line-height: 1.6;
        }
        table { border-collapse: collapse; width: 100%; }
        .container {
            max-width: 580px;
            background: #fff;
            margin: 0 auto;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 18px rgba(122, 108, 255, 0.12);
        }

        /* Header */
        .header {
            background: linear-gradient(135deg, #6C63FF 0%, #66C6FF 100%);
            padding: 60px 40px;
            text-align: center;
        }
        .logo {
            color: #fff;
            font-size: 20px;
            text-transform: uppercase;
            letter-spacing: 3px;
            font-weight: 300;
        }

        .content {
            padding: 60px 40px;
        }

        .greeting {
            font-size: 24px;
            font-weight: 300;
            color: #6C63FF;
            margin-bottom: 30px;
        }

        .description {
            font-size: 15px;
            margin-bottom: 35px;
            color: #555;
        }

        /* Progress Box */
        .progress-box {
            background: linear-gradient(135deg, #F2F0FF 0%, #F8F9FF 100%);
            padding: 35px;
            border-radius: 12px;
            border: 2px solid #D5D1FF;
            box-shadow: 0 4px 12px rgba(108, 99, 255, 0.10);
            margin-bottom: 40px;
        }
        .progress-title {
            font-size: 22px;
            color: #6C63FF;
            font-weight: 500;
            margin-bottom: 15px;
        }
        .progress-item {
            font-size: 14px;
            color: #444;
            margin-bottom: 12px;
        }
        .highlight {
            color: #6C63FF;
            font-weight: 600;
        }

        /* CTA */
        .action-box {
            background: linear-gradient(135deg, #EAF3FF 0%, #F3F8FF 100%);
            padding: 35px;
            border-radius: 10px;
            border: 2px solid #66A8FF;
            text-align: center;
            margin-bottom: 40px;
        }
        .action-text {
            font-size: 14px;
            color: #555;
            margin-bottom: 15px;
        }
        .action-link {
            color: #357BFF;
            text-decoration: none;
            font-weight: 600;
            border-bottom: 2px solid #6C63FF;
            transition: .3s ease;
        }
        .action-link:hover {
            color: #6C63FF;
            border-bottom-color: #357BFF;
        }

        .divider {
            height: 1px;
            margin: 40px 0;
            background: linear-gradient(90deg, transparent, #6C63FF, transparent);
        }

        /* Footer */
        .footer {
            background: linear-gradient(135deg, #EEF1FF 0%, #F4F7FF 100%);
            border-top: 2px solid #6C63FF;
            padding: 50px 40px;
            text-align: center;
        }
        .footer-text {
            font-size: 12px;
            color: #6C63FF;
            margin-bottom: 12px;
        }
        .footer-divider {
            height: 1px;
            background: linear-gradient(90deg, transparent, #FFD54F, transparent);
            margin: 25px 0;
        }

        @media (max-width: 600px) {
            .content, .header, .footer {
                padding: 40px 25px;
            }
            .progress-box, .action-box {
                padding: 25px;
            }
        }
    </style>
</head>

<body>
<table role="presentation">
    <tr>
        <td align="center" style="padding: 30px 10px;">
            <table role="presentation" class="container">
                <!-- HEADER -->
                <tr>
                    <td class="header">
                        <div class="logo">{{brandName}}</div>
                    </td>
                </tr>

                <!-- CONTENT -->
                <tr>
                    <td class="content">
                        <p class="greeting">Xin chào <b>{{studentName}}</b>,</p>

                        <p class="description">
                            Đây là <span class="highlight">báo cáo tiến độ học tập</span> của bạn trong giai đoạn gần đây.
                            Chúng tôi rất vui khi thấy sự nỗ lực của bạn!
                        </p>

                        <!-- PROGRESS SUMMARY -->
                        <div class="progress-box">
                            <div class="progress-title">Tổng quan tiến độ</div>

                            <p class="progress-item">📘 Khóa học: <b>{{courseName}}</b></p>
                            <p class="progress-item">📅 Giai đoạn: {{reportPeriod}}</p>
                            <p class="progress-item">🎯 Mức độ hoàn thành: <span class="highlight">{{completionRate}}%</span></p>
                            <p class="progress-item">⏳ Thời gian học: {{studyHours}} giờ</p>
                            <p class="progress-item">⭐ Điểm trung bình: <span class="highlight">{{averageScore}}</span></p>
                        </div>

                        <!-- CTA -->
                        <div class="action-box">
                            <p class="action-text">Xem chi tiết quá trình học của bạn tại đây:</p>
                            <a href="{{detailLink}}" class="action-link">Xem chi tiết tiến độ</a>
                        </div>

                        <p class="description">
                            Hãy tiếp tục duy trì phong độ học tập tuyệt vời này.  
                            Chúng tôi luôn đồng hành cùng bạn!
                        </p>

                        <div class="divider"></div>
                    </td>
                </tr>

                <!-- FOOTER -->
                <tr>
                    <td class="footer">
                        <p class="footer-text">© {{year}} {{brandName}}. Mọi quyền được bảo lưu.</p>
                        <div class="footer-divider"></div>
                        <p class="footer-text">
                            <a href="{{supportLink}}" style="color:#6C63FF; text-decoration:none;">
                                Liên hệ hỗ trợ
                            </a>
                        </p>
                    </td>
                </tr>

            </table>
        </td>
    </tr>
</table>
</body>
</html>
