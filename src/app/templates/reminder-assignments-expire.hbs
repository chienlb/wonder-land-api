<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nhắc sắp hết hạn bài tập</title>

    <style>
        body {
            margin: 0; padding: 0;
            background: #F7F9FF;
            font-family: 'Segoe UI', sans-serif;
            color: #3A3A3A;
            line-height: 1.6;
        }

        .container {
            max-width: 580px; margin: 0 auto;
            background: #fff; border-radius: 14px;
            overflow: hidden;
            box-shadow: 0 4px 16px rgba(100, 120, 255, 0.12);
        }

        .header {
            background: linear-gradient(135deg, #3A6BFF 0%, #76A7FF 100%);
            padding: 55px 40px; text-align: center;
        }
        .logo {
            color: #fff; font-size: 20px;
            letter-spacing: 3px; font-weight: 300;
            text-transform: uppercase;
        }

        .content { padding: 60px 40px; }
        .greeting {
            font-size: 24px; color: #3A6BFF;
            margin-bottom: 20px; font-weight: 300;
        }
        .description {
            font-size: 15px; color: #555;
            margin-bottom: 30px;
        }
        .highlight { color: #FF7A3D; font-weight: 600; }

        /* Deadline box */
        .deadline-box {
            background: #FFF6EE;
            border: 2px solid #FFB48A;
            padding: 35px;
            border-radius: 12px;
            margin-bottom: 40px;
        }
        .deadline-title {
            font-size: 20px;
            color: #FF7A3D;
            font-weight: 600;
            margin-bottom: 15px;
        }
        .deadline-item {
            font-size: 14px;
            color: #554;
            margin-bottom: 10px;
        }

        /* CTA */
        .action-box {
            background: #E9F1FF;
            border: 2px solid #3A6BFF;
            padding: 35px; text-align: center;
            border-radius: 12px; margin-bottom: 40px;
        }
        .action-link {
            color: #3A6BFF; font-weight: 600;
            border-bottom: 2px solid #FF7A3D;
            text-decoration: none; padding-bottom: 2px;
            transition: .3s;
        }
        .action-link:hover {
            color: #FF7A3D;
            border-bottom-color: #3A6BFF;
        }

        .footer {
            background: #EEF2FF; padding: 45px;
            text-align: center;
            border-top: 2px solid #3A6BFF;
        }
        .footer-text { font-size: 12px; color: #3A6BFF; }
    </style>
</head>

<body>
<table width="100%">
    <tr><td align="center" style="padding: 30px 10px;">
        <table class="container">

            <tr><td class="header"><div class="logo">{{brandName}}</div></td></tr>

            <tr><td class="content">

                <p class="greeting">Xin chào <b>{{studentName}}</b>,</p>

                <p class="description">
                    Đây là lời nhắc rằng bài tập <span class="highlight">{{assignmentTitle}}</span> của bạn  
                    đang <span class="highlight">sắp đến hạn nộp</span>. Hãy hoàn thành trước khi quá muộn nhé!
                </p>

                <div class="deadline-box">
                    <div class="deadline-title">Thông tin bài tập</div>
                    <p class="deadline-item">📘 Khóa học: <b>{{courseName}}</b></p>
                    <p class="deadline-item">⏳ Hạn nộp: <b>{{deadline}}</b></p>
                    <p class="deadline-item">🎯 Trạng thái: {{status}}</p>
                </div>

                <div class="action-box">
                    <p>Nhấn vào đây để hoàn thành bài tập:</p>
                    <a href="{{assignmentLink}}" class="action-link">Làm bài ngay</a>
                </div>

                <p class="description">
                    Chúc bạn hoàn thành bài tập thật tốt!  
                    Nếu cần hỗ trợ, đừng ngần ngại liên hệ đội ngũ của chúng tôi.
                </p>

            </td></tr>

            <tr><td class="footer">
                <p class="footer-text">© {{year}} {{brandName}}. Mọi quyền được bảo lưu.</p>
            </td></tr>

        </table>
    </td></tr>
</table>
</body>
</html>
