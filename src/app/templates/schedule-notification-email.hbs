<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Lịch học hôm nay</title>
    <style type="text/css">
        body {
            margin: 0;
            padding: 0;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Helvetica Neue', sans-serif;
            background: #FFFEF5;
            line-height: 1.6;
            color: #3a3a3a;
        }
        table {
            border-collapse: collapse;
            width: 100%;
        }
        img {
            max-width: 100%;
            height: auto;
            display: block;
        }
        .container {
            max-width: 580px;
            margin: 0 auto;
            background-color: #ffffff;
            box-shadow: 0 4px 16px rgba(45, 157, 217, 0.12);
            border-radius: 12px;
            overflow: hidden;
        }
        /* header gradient sky blue sang coral */
        .header {
            background: linear-gradient(135deg, #2D9DD9 0%, #FF6B6B 100%);
            padding: 80px 45px 60px;
            text-align: center;
        }
        .logo {
            font-size: 18px;
            font-weight: 300;
            color: #ffffff;
            letter-spacing: 3px;
            text-transform: uppercase;
        }
        .content {
            padding: 70px 40px;
        }
        /* greeting màu sky blue */
        .greeting {
            font-size: 28px;
            font-weight: 400;
            color: #2D9DD9;
            margin: 0 0 45px 0;
            letter-spacing: -0.3px;
            line-height: 1.3;
        }
        .description {
            font-size: 15px;
            color: #5a5a5a;
            line-height: 1.8;
            margin: 0 0 32px 0;
        }
        /* highlight màu coral orange */
        .highlight {
            color: #FF6B6B;
            font-weight: 600;
        }
        /* schedule-item background gradient mint nhạt */
        .schedule-item {
            background: linear-gradient(135deg, #E0F7F4 0%, #F0FFFE 100%);
            border-left: 5px solid #4ECDC4;
            padding: 35px 40px;
            margin: 30px 0;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(78, 205, 196, 0.12);
            position: relative;
        }
        /* schedule-time màu mint green */
        .schedule-time {
            font-size: 12px;
            color: #4ECDC4;
            letter-spacing: 2px;
            text-transform: uppercase;
            margin-bottom: 12px;
            font-weight: 600;
        }
        /* schedule-title màu sky blue */
        .schedule-title {
            font-size: 19px;
            color: #2D9DD9;
            font-weight: 500;
            margin: 0 0 15px 0;
            letter-spacing: -0.2px;
        }
        .schedule-details {
            font-size: 14px;
            color: #5a5a5a;
            line-height: 1.9;
            margin: 0;
        }
        /* divider gradient mint sang sky blue */
        .divider {
            height: 1px;
            background: linear-gradient(90deg, transparent, #4ECDC4, transparent);
            margin: 50px 0;
        }
        .action-section {
            text-align: center;
            margin: 45px 0;
        }
        /* action-link màu sky blue */
        .action-link {
            color: #2D9DD9;
            text-decoration: none;
            font-weight: 600;
            border-bottom: 2px solid #FF6B6B;
            transition: all 0.3s ease;
        }
        .action-link:hover {
            color: #FF6B6B;
            border-bottom-color: #2D9DD9;
        }
        /* footer gradient mint sang sky blue */
        .footer {
            background: linear-gradient(135deg, #E0F7F4 0%, #E8F8FC 100%);
            padding: 60px 40px;
            text-align: center;
            border-top: 2px solid #4ECDC4;
            border-radius: 0 0 12px 12px;
        }
        /* footer-text màu sky blue */
        .footer-text {
            font-size: 12px;
            color: #2D9DD9;
            margin: 0 0 12px 0;
            line-height: 1.9;
            letter-spacing: 0.4px;
            font-weight: 500;
        }
        .footer-divider {
            height: 1px;
            background: linear-gradient(90deg, transparent, #FFD700, transparent);
            margin: 30px 0;
        }
        @media (max-width: 600px) {
            .content {
                padding: 50px 25px;
            }
            .header {
                padding: 50px 25px 40px;
            }
            .greeting {
                font-size: 22px;
            }
            .schedule-item {
                padding: 25px;
            }
            .footer {
                padding: 50px 25px;
            }
        }
    </style>
</head>
<body>
    <table role="presentation">
        <tr>
            <td align="center" style="padding: 30px 10px;">
                <table role="presentation" class="container">
                    <tr>
                        <td class="header">
                            <div class="logo">{{brandName}}</div>
                        </td>
                    </tr>
                    <tr>
                        <td class="content">
                            <p class="greeting">Xin chào {{userName}},</p>
                            <p class="description">
                                Bạn có <span class="highlight">{{scheduleCount}} buổi học</span> hôm nay. Dưới đây là lịch học của bạn:
                            </p>
                            {{#each schedules}}
                            <div class="schedule-item">
                                <div class="schedule-time">{{this.time}}</div>
                                <div class="schedule-title">{{this.subject}}</div>
                                <div class="schedule-details">
                                    Giáo viên: {{this.instructor}}<br>
                                    {{#if this.location}}Địa điểm: {{this.location}}{{/if}}
                                </div>
                            </div>
                            {{/each}}
                            <div class="divider"></div>
                            <div class="action-section">
                                <p class="description">
                                    Xem toàn bộ lịch học bằng cách <a href="{{dashboardLink}}" class="action-link">truy cập bảng điều khiển</a>
                                </p>
                            </div>
                        </td>
                    </tr>
                    <tr>
                        <td class="footer">
                            <p class="footer-text">
                                © {{year}} {{brandName}}. Bảo lưu mọi quyền.
                            </p>
                            <div class="footer-divider"></div>
                            <p class="footer-text">
                                <a href="{{settingsLink}}" style="color: #2D9DD9; text-decoration: none;">Quản lý thông báo</a>
                            </p>
                        </td>
                    </tr>
                </table>
            </td>
        </tr>
    </table>
</body>
</html>
