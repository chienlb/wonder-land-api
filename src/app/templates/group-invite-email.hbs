<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Được thêm vào nhóm</title>
    <style type="text/css">
        body {
            margin: 0;
            padding: 0;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Helvetica Neue', sans-serif;
            background: #FFFEF5;
            line-height: 1.6;
            color: #3a3a3a;
        }
        table {
            border-collapse: collapse;
            width: 100%;
        }
        img {
            max-width: 100%;
            height: auto;
            display: block;
        }
        .container {
            max-width: 580px;
            margin: 0 auto;
            background-color: #ffffff;
            box-shadow: 0 4px 16px rgba(45, 157, 217, 0.12);
            border-radius: 12px;
            overflow: hidden;
        }
        /* header gradient sky blue sang mint */
        .header {
            background: linear-gradient(135deg, #2D9DD9 0%, #4ECDC4 100%);
            padding: 70px 40px 50px;
            text-align: center;
        }
        .logo {
            font-size: 18px;
            font-weight: 300;
            color: #ffffff;
            letter-spacing: 3px;
            text-transform: uppercase;
        }
        .content {
            padding: 70px 40px;
        }
        /* greeting màu sky blue */
        .greeting {
            font-size: 24px;
            font-weight: 300;
            color: #2D9DD9;
            margin: 0 0 35px 0;
            letter-spacing: 0.5px;
        }
        .description {
            font-size: 15px;
            color: #5a5a5a;
            line-height: 1.8;
            margin: 0 0 32px 0;
        }
        /* highlight màu coral */
        .highlight {
            color: #FF6B6B;
            font-weight: 600;
        }
        /* group-card background gradient soft pink nhạt */
        .group-card {
            background: linear-gradient(135deg, #FFE8E6 0%, #FFF5F5 100%);
            border: 2px solid #FFB6C1;
            border-radius: 12px;
            padding: 50px 40px;
            margin: 50px 0;
            position: relative;
            text-align: center;
            box-shadow: 0 4px 16px rgba(255, 182, 193, 0.15);
        }
        .group-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #FFD700 0%, #4ECDC4 100%);
            border-radius: 12px 12px 0 0;
        }
        .group-icon {
            font-size: 48px;
            margin-bottom: 20px;
            line-height: 1;
        }
        /* group-name màu sky blue */
        .group-name {
            font-size: 24px;
            color: #2D9DD9;
            font-weight: 500;
            margin: 20px 0 18px 0;
            letter-spacing: -0.2px;
        }
        .group-description {
            font-size: 14px;
            color: #666666;
            line-height: 1.8;
            margin: 0;
        }
        /* members-section background gradient mint nhạt */
        .members-section {
            margin: 50px 0;
            background: linear-gradient(135deg, #E0F7F4 0%, #F0FFFE 100%);
            border: 2px solid #4ECDC4;
            padding: 35px 40px;
            border-radius: 8px;
        }
        /* members-title màu mint green */
        .members-title {
            font-size: 12px;
            color: #4ECDC4;
            letter-spacing: 2px;
            text-transform: uppercase;
            margin: 0 0 25px 0;
            font-weight: 700;
        }
        .member-item {
            font-size: 14px;
            color: #666666;
            margin: 0 0 12px 0;
            padding-left: 20px;
            position: relative;
        }
        /* member-item bullet màu coral */
        .member-item::before {
            content: '•';
            position: absolute;
            left: 0;
            color: #FF6B6B;
            font-weight: 600;
        }
        .action-section {
            margin: 40px 0;
            text-align: center;
        }
        .action-text {
            font-size: 14px;
            color: #666666;
            line-height: 1.8;
            margin: 0 0 20px 0;
        }
        /* action-link màu sky blue */
        .action-link {
            color: #2D9DD9;
            text-decoration: none;
            font-weight: 600;
            border-bottom: 2px solid #FF6B6B;
            transition: all 0.3s ease;
        }
        .action-link:hover {
            color: #FF6B6B;
            border-bottom-color: #2D9DD9;
        }
        /* divider gradient mint sang sky blue */
        .divider {
            height: 1px;
            background: linear-gradient(90deg, transparent, #4ECDC4, transparent);
            margin: 50px 0;
        }
        /* footer gradient mint sang sky blue */
        .footer {
            background: linear-gradient(135deg, #E0F7F4 0%, #E8F8FC 100%);
            padding: 60px 40px;
            text-align: center;
            border-top: 2px solid #4ECDC4;
            border-radius: 0 0 12px 12px;
        }
        /* footer-text màu sky blue */
        .footer-text {
            font-size: 12px;
            color: #2D9DD9;
            margin: 0 0 12px 0;
            line-height: 1.9;
            letter-spacing: 0.4px;
            font-weight: 500;
        }
        .footer-divider {
            height: 1px;
            background: linear-gradient(90deg, transparent, #FFD700, transparent);
            margin: 30px 0;
        }
        @media (max-width: 600px) {
            .content {
                padding: 50px 25px;
            }
            .header {
                padding: 50px 25px 40px;
            }
            .greeting {
                font-size: 22px;
            }
            .group-card {
                padding: 35px 25px;
            }
            .members-section {
                padding: 25px;
            }
            .footer {
                padding: 50px 25px;
            }
        }
    </style>
</head>
<body>
    <table role="presentation">
        <tr>
            <td align="center" style="padding: 30px 10px;">
                <table role="presentation" class="container">
                    <tr>
                        <td class="header">
                            <div class="logo">{{brandName}}</div>
                        </td>
                    </tr>
                    <tr>
                        <td class="content">
                            <p class="greeting">Xin chào {{userName}},</p>
                            <p class="description">
                                Bạn đã được thêm vào nhóm <span class="highlight">{{groupName}}</span> bởi {{invitedByName}}.
                            </p>
                            <div class="group-card">
                                <div class="group-icon">👥</div>
                                <div class="group-name">{{groupName}}</div>
                                <div class="group-description">{{groupDescription}}</div>
                            </div>
                            <div class="members-section">
                                <p class="members-title">Thành viên nhóm</p>
                                {{#each groupMembers}}
                                <div class="member-item">{{this.name}}</div>
                                {{/each}}
                            </div>
                            <div class="divider"></div>
                            <div class="action-section">
                                <p class="action-text">Truy cập nhóm để xem thêm chi tiết và bắt đầu cộng tác</p>
                                <p class="action-text">
                                    <a href="{{groupLink}}" class="action-link">Xem nhóm</a>
                                </p>
                            </div>
                            <p class="description">
                                Trong nhóm, bạn có thể chia sẻ tài liệu, thảo luận và cùng nhau hoàn thành các nhiệm vụ một cách hiệu quả.
                            </p>
                        </td>
                    </tr>
                    <tr>
                        <td class="footer">
                            <p class="footer-text">
                                © {{year}} {{brandName}}. Bảo lưu mọi quyền.
                            </p>
                            <div class="footer-divider"></div>
                            <p class="footer-text">
                                <a href="{{leaveGroupLink}}" style="color: #2D9DD9; text-decoration: none;">Rời khỏi nhóm</a>
                            </p>
                        </td>
                    </tr>
                </table>
            </td>
        </tr>
    </table>
</body>
</html>
